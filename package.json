{
  "name": "expense-management",
  "version": "1.0.0",
  "description": "BluestoneX Expense Reimbursement System",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/hana": "^1.9.2",
    "@sap/cds": "^8",
    "@sap/xsenv": "^5",
    "@sap/xssec": "^4",
    "express": "^4",
    "hdb": "^0.19",
    "nodemailer": "^9.0.3",
    "pdfkit": "^0.19.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^8",
    "axios": "^1.18.0",
    "better-sqlite3": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build --production",
    "test": "node --test test/*.test.js",
    "test:ui": "echo \"UI5 OPA5/QUnit tests run in a browser/Karma runner, not node — see app/*/webapp/test/ and test/e2e/\""
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": ":memory:"
        },
        "[production]": {
          "kind": "hana"
        }
      },
      "alert-notification": {
        "[production]": {
          "kind": "rest",
          "credentials": {}
        }
      }
    },
    "features": {
      "preserve_computed": true
    }
  }
}
